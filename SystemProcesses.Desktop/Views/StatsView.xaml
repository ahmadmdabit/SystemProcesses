<Window
    x:Class="SystemProcesses.Desktop.Views.StatsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:converters="clr-namespace:SystemProcesses.Desktop.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:SystemProcesses.Desktop.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewmodels="clr-namespace:SystemProcesses.Desktop.ViewModels"
    Title="System Stats"
    Width="Auto"
    Height="46"
    AllowsTransparency="False"
    Background="Transparent"
    KeyUp="WindowKeyUp"
    ResizeMode="NoResize"
    ShowInTaskbar="False"
    SizeToContent="Width"
    WindowStyle="None"
    mc:Ignorable="d">

    <Window.DataContext>
        <viewmodels:StatsViewModel />
    </Window.DataContext>

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Resources/UIStrings.xaml" />
                <ResourceDictionary Source="/Resources/Styles.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Window.ContextMenu>
        <ContextMenu
            Placement="Relative"
            PlacementRectangle="0,0,0,0"
            Style="{StaticResource ContextMenuStyle}"
            VerticalOffset="-8">
            <MenuItem
                Command="{Binding ShowApplicationCommand}"
                Header="{StaticResource ContextMenuShow}"
                Style="{StaticResource MenuItemStyle}" />
            <MenuItem
                Command="{Binding ExitApplicationCommand}"
                Header="{StaticResource ContextMenuExit}"
                Style="{StaticResource MenuItemStyle}" />
        </ContextMenu>
    </Window.ContextMenu>

    <Border
        x:Name="RootBorder"
        Height="46"
        Padding="5,5,2,5"
        Background="{StaticResource WidgetBrush}"
        BorderBrush="{StaticResource WidgetBorderBrush}"
        BorderThickness="1"
        CornerRadius="5"
        MouseDown="BorderMouseDown"
        SnapsToDevicePixels="True">
        <StackPanel Orientation="Horizontal">
            <!--  CPU  -->
            <Border
                Background="{StaticResource IconBorder.Background}"
                BorderBrush="{StaticResource AccentBrush}"
                Style="{StaticResource IconBorder}">
                <StackPanel Orientation="Vertical" Style="{StaticResource IconStack}">
                    <TextBlock Style="{StaticResource GreenTextBlock2}" Text="{StaticResource StatsViewCPU}" />
                    <TextBlock Style="{StaticResource GreenTextBlock}" Text="{Binding TotalCpuUsage, Converter={StaticResource CpuPercentageConverter}, FallbackValue=0%}" />
                </StackPanel>
            </Border>

            <!--  RAM  -->
            <Border Style="{StaticResource IconBorder}">
                <StackPanel Orientation="Vertical" Style="{StaticResource IconStack}">
                    <TextBlock Style="{StaticResource GreenTextBlock2}" Text="{StaticResource StatsViewRAM}" />
                    <TextBlock Style="{StaticResource GreenTextBlock}" Text="{Binding RamPercent, Converter={StaticResource CpuPercentageConverter}, FallbackValue=0%}" />
                </StackPanel>
            </Border>

            <!--  Virtual Memory  -->
            <Border Style="{StaticResource IconBorder}">
                <StackPanel Orientation="Vertical" Style="{StaticResource IconStack}">
                    <TextBlock Style="{StaticResource GreenTextBlock2}" Text="{StaticResource StatsViewVM}" />
                    <TextBlock Style="{StaticResource GreenTextBlock}" Text="{Binding VMPercent, Converter={StaticResource CpuPercentageConverter}, FallbackValue=0%}" />
                </StackPanel>
            </Border>

            <!--  Disk Activity  -->
            <Border Style="{StaticResource IconBorder}">
                <StackPanel Orientation="Vertical" Style="{StaticResource IconStack}">
                    <TextBlock Style="{StaticResource GreenTextBlock2}" Text="{StaticResource StatsViewDisk}" />
                    <TextBlock Style="{StaticResource GreenTextBlock}" Text="{Binding DiskActivePercent, Converter={StaticResource CpuPercentageConverter}, FallbackValue=0%}" />
                </StackPanel>
            </Border>

            <!--  Dynamic Drive List  -->
            <ItemsControl ItemsSource="{Binding Drives}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Horizontal" />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border
                            Background="{StaticResource IconBorder.Background}"
                            BorderBrush="{StaticResource AccentBrush}"
                            Cursor="Hand"
                            Style="{StaticResource IconBorder}">
                            <Border.InputBindings>
                                <MouseBinding
                                    Command="{Binding OpenDriveCommand}"
                                    CommandParameter="{Binding}"
                                    MouseAction="LeftClick" />
                            </Border.InputBindings>
                            <StackPanel Orientation="Vertical">
                                <TextBlock Style="{StaticResource GreenTextBlock2}" Text="{Binding DriveLabel}" />
                                <TextBlock Style="{StaticResource GreenTextBlock}" Text="{Binding FreeBytes, Converter={StaticResource BytesToAutoFormatConverter}}" />
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </StackPanel>
    </Border>
</Window>
